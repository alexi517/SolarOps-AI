# Virtual environment
.venv/
venv/

# Python caches / build artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
.coverage
htmlcov/

# OS / editor noise (keep .vscode/ — it's shared project config)
.DS_Store
.idea/

# Local secrets — copy .env.example to .env and fill it in (Phase 8 brief §3)
.env

# Local MLflow tracking state (only relevant to local, non-Docker runs)
mlruns/
mlflow.db

# Personal learning/reference docs — kept locally, not for the public repo
/HARDWARE_INTEGRATION.md
/LEARNING_PATH.md
/ONBOARDING.md
/PROJECT_DEEP_DIVE.md
/docs/CODE-WALKTHROUGH.md
